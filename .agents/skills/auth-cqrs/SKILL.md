---
name: auth-cqrs
description: Use for command handlers, query handlers, projections, process managers, controller endpoints, and write/read model separation in the auth service.
---

# Auth CQRS

Canonical rules: `../../../skills/cqrs.md`

This Codex skill is a format wrapper only. Before working, read and apply the canonical rules above. Do not duplicate shared project rules here; update the canonical file instead.
