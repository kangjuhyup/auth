---
name: auth-naming
description: Use when adding or renaming files, DTOs, commands, queries, controllers, view models, infrastructure payloads, or classes in the auth service.
---

# Auth Naming

Canonical rules: `../../../skills/naming.md`

This Codex skill is a format wrapper only. Before working, read and apply the canonical rules above. Do not duplicate shared project rules here; update the canonical file instead.
