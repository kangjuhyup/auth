---
name: auth-oidc-protocol
description: Use for OIDC/OAuth2 work in service including node-oidc-provider config, findAccount, claims, PKCE, Grant/Interaction flow, JWKS, resource indicators, token format, and SSO protocol integration.
---

# Auth OIDC Protocol

Canonical rules: `../../../skills/oidc-protocol.md`

This Codex skill is a format wrapper only. Before working, read and apply the canonical rules above. Do not duplicate shared project rules here; update the canonical file instead.
