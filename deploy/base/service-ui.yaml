apiVersion: v1
kind: Service
metadata:
  name: auth-ui
  labels:
    app.kubernetes.io/name: auth-ui
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: auth-ui
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
