name: auth_platform_flutter
description: Flutter integration SDK for self-hosted Auth Platform OIDC tenants.
version: 0.1.0-dev.1
repository: https://github.com/kangjuhyup/auth/tree/main/sdks/flutter
issue_tracker: https://github.com/kangjuhyup/auth/issues

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_appauth: ^9.0.1
  flutter_secure_storage: ^10.3.1
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
