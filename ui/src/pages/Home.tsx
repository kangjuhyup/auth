import { Typography } from 'antd';

const { Title } = Typography;

export function HomePage() {
  return (
    <div style={{ padding: 24 }}>
      <Title level={2}>Auth Service</Title>
    </div>
  );
}
